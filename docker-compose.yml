version: '3.3'

services:
  linear-webhook:
    build: .
    ports:
      - "9900:8000"
    environment:
      # 数据库配置
      - DATABASE_URL=sqlite:///./linear_webhook.db
      
      # 服务器配置
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=false
      - LOG_LEVEL=info
      
      # Aider 配置 - DeepSeek
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=https://api.deepseek.com/v1
      - OPENAI_MODEL=deepseek-chat
      - AIDER_OPENAI_API_BASE=https://api.deepseek.com/v1
      - AIDER_OPENAI_API_KEY=${OPENAI_API_KEY}
      - AIDER_OPENAI_MODEL=deepseek-chat
      
      # Aider 其他配置
      - AIDER_WORK_DIR=/app
      - AIDER_EDITOR=code
      - AIDER_AUTO_COMMITS=true
      - AIDER_DARK_MODE=false
      - AIDER_YES=false
      - AIDER_SAFE_MODE=true
      
      # 可选：其他 AI 模型配置
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - COHERE_API_KEY=${COHERE_API_KEY}
      
    volumes:
      # 持久化数据库
      - ./data:/app/data
      # 持久化 Aider 配置
      - ./aider_config:/app/.aider
    restart: unless-stopped
